# Dipendenze del sidecar. PyTorch e `nemotron-ocr` vanno installati PRIMA:
# il pacchetto ufficiale contiene un'estensione C++/CUDA e deve essere
# compilato dal repository NVIDIA contro il PyTorch e il toolkit locali.
numpy>=1.26
pillow>=12.0
